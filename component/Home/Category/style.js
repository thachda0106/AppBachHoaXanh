import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 90,
        display: 'flex',
        flexDirection: 'row',
        overflow: 'scroll',
        marginTop: 12
    }
})