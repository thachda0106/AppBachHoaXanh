import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    width: "100%",
    height: 80,
  },
  swiper: {
    margin: 0,
    padding: 0,
  },
  bannerImg: {
    resizeMode: "contain",
    width: "100%",
    marginTop: -60
  },
});
